<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/startActivity">

    <fragment
        android:id="@+id/startActivity"
        android:name="com.example.dingbats.StartActivity"
        android:label="activity_start"
        tools:layout="@layout/fragment_start"
        >
        <action
            android:id="@+id/action_startActivity_to_gameActivity"
            app:destination="@id/gameActivity" />
    </fragment>
    <fragment
        android:id="@+id/gameActivity"
        android:name="com.example.dingbats.GameActivity"
        android:label="activity_game"
        tools:layout="@layout/fragment_game"
        />
</navigation>